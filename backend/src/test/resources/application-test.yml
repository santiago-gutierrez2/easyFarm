spring:
  datasource:
    url: jdbc:mysql://localhost/easyfarmtest?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid
    username: easyfarm
    password: easyfarm
    max-active: 1
    max-idle: 1
